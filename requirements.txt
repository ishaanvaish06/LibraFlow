# Runtime
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-dateutil>=2.8.2

# Security
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 relies on bcrypt's pre-4.1 password truncation behaviour;
# bcrypt>=4.1 raises on >72-byte passwords and breaks passlib's import-time
# backend detection. Pin below 4.1.
bcrypt>=4.0.0,<4.1

# Persistence
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Caching
redis>=5.0.0

# Machine learning (classical)
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0

# Dev / Testing / Benchmarking
pytest>=8.0.0
requests>=2.31.0
httpx>=0.27.0
pytest-cov>=5.0.0
locust>=2.25.0